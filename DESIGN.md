# Story Guard Desktop Design

## 현재 시각 기준 — 2026-09-14

현재 기준은 [Figma v3 Dark](https://www.figma.com/design/ShVDj8aEDoBYnzWqUNqray/StoryGuard?node-id=50-5)와 [Light](https://www.figma.com/design/ShVDj8aEDoBYnzWqUNqray/StoryGuard?node-id=55-82)다.
사용자 요청으로 기존 청록·아이보리 시각 기준을 라벤더·차콜 / 라벤더·종이색 테마로 갱신했다.

- 색상은 `src/theme.css`의 의미별 토큰에서 관리한다. 라이트·다크·시스템 선택은 기기에 저장하며, 시스템 선택은 OS 설정 변경을 따른다.
- 기존 공통 UI와 데이터 흐름을 유지하고 `src/storyguard.css`에서 상단 탐색, 읽기용 서체, 여백, 그래프와 하단 근거 구성을 적용한다.
- UI 서체는 기존 Pretendard를 재사용하고 제목·원문에는 로컬 Noto Serif KR을 사용한다. 배경 이미지는 그래프 영역의 장식이며 원고·노드·근거는 실제 데이터로 렌더링한다.
- 관계 지도는 유형별 색을 가진 작은 원형 노드와 바깥 이름표를 사용한다. 유형은 범례·선택 목록·포커스 시 문구로도 확인할 수 있다.
- 선택한 관계의 원문 인용을 먼저 보여주고, 관계 상세와 전체 근거는 펼쳐서 확인한다. 작가 판단과 원문 이동 기능을 유지한다.
- **교집합·합집합 탐색, 기준 인물 추가 등 새로운 조회 로직은 이번 범위에서 제외한다.** Figma의 예시 조건·인물·문장을 실제 결과처럼 하드코딩하지 않는다.
- 작은 창에서는 페이지를 세로로 스크롤해 모든 기능에 접근한다. 고정 시안의 높이에 맞추기 위해 원문이나 버튼을 자르지 않는다.

구현·검증 범위와 작업 단위는 [Figma v3 반영 기록](docs/figma-v3-implementation.md)을 참고한다.

아래 2026-09-11 기록의 **시각 규칙(청록 색상, 사이드 메뉴, 유형별 도형)은 이전 기준**이다. 데이터·근거·작가 판단의 기능 계약은 유지하며, 충돌하는 시각 규칙에는 위 현재 기준을 적용한다.

## 관계 지도 수정 — 2026-09-11

관계 지도는 모든 추출 후보를 한 캔버스에 밀어 넣는 화면이 아니라 관계의 건강 상태를 점검하는 작업 공간이다. 상단의 관계 건강도는 연결된 엔티티, 고립 후보, 근거 부족, 미분류 관계, 충돌, 회차 변화, 중간 공백, 명시적 단절을 각각 집계한다. 집계 수치는 오류 확률이 아니라 현재 확인할 작업의 수다.

관계 상태는 다음을 구분한다.

- 고립 후보: 엔티티는 발견됐지만 검증된 관계가 없음
- 근거 부족: 양 끝 노드는 있으나 인용문 또는 chunk 근거가 없음
- 미분류 관계: `관계`, `관련`, `co_occurs`처럼 의미 있는 술어로 해석되지 않음
- 변화: 회차에 따라 같은 쌍의 관계 술어가 달라짐
- 충돌: 같은 쌍에 협력과 적대처럼 양립하기 어려운 술어가 함께 존재함
- 미언급: 후속 회차에 등장하지 않은 것만으로 단절로 판단하지 않음
- 명시적 단절: `단절`, `해제`, `결별`, `종료`처럼 원문 관계 유형에 단절 표현이 있을 때만 단절 배지를 표시
- 중간 공백 후보: 앞뒤 회차에는 같은 관계가 관찰되지만 사이 회차에 근거가 없을 때 표시하며, 단절로 확정하지 않는다

`전체 데이터`는 모든 후보에 접근하는 탐색 모드이며, 캔버스에 모든 노드를 축소해 그리는 모드가 아니다. 핵심 관계망은 연결된 관계만 그리고, 고립 후보는 접을 수 있는 별도 목록으로 제공한다. 관계선은 확정 관계(실선), 추론·근거 부족(점선), 미분류(점선과 황갈색), 충돌(적갈색 점선)으로 구분하고 색상과 문구를 함께 표시한다.

`점검 필요` 필터는 약한 관계, 미분류 관계, 근거가 없는 관계와 회차상 변화·중간 공백·명시적 단절 관계를 남기고, 관계가 하나도 없는 고립 후보도 별도 노드·목록으로 보존한다. 이 필터는 오류를 확정하는 기능이 아니라 작가가 먼저 확인할 대상을 좁히는 기능이다. 자동 배치는 연결 요소별 중심-방사형으로 배치하고, 각 홉의 노드를 부모 각도 기준으로 정렬해 선 교차를 줄인 뒤 카드 충돌을 해소한다. 일반 크기의 원고에서는 최소 46% 확대를 유지해 전체 지도가 지나치게 작아지지 않게 한다.

- 관계가 있는 대상만 메인 네트워크에 표시한다. 연결이 없는 추출 후보는 접이식 목록에서 확인하며 원본을 삭제하거나 임의 관계를 만들지 않는다.
- 실제 연결에 따른 방사형 계층 배치와 카드 간격 계산을 사용한다. 각 묶음의 최고 연결 노드를 중심 앵커로 두고 직접 연결·간접 연결을 고리로 펼친다. 과거 격자 좌표는 사용하지 않는다.
- 관계 유형과 방향을 선에 표시한다. 동일 쌍의 여러 주장은 캔버스에서 대표선 하나로 집계하고 후보 수·양방향 여부를 표시하며, 개별 근거는 오른쪽 패널에서 전환한다.
- 필터·작품·창 크기 변경 시 표시 중인 네트워크를 중앙에 다시 맞춘다. 단순 선택은 좌표를 바꾸지 않는다.
- 선택 대상 주변 보기, 전체 관계망 복귀, 확대·축소·화면 맞춤·자동 배치를 제공한다.
- 청록 인물 원형, 나머지 유형은 밝은 카드와 유형 문구를 쓴다. 출현 상태만으로 주황색 경고를 표시하지 않는다.
- 연결도가 높은 노드는 크기를 키워 중심성을 드러내고, 휴면·소멸 후보는 투명도를 낮춘다. 크기·투명도는 현재 필터 범위에서 다시 계산한다.
- 오른쪽 패널은 실제 선택한 관계와 원문 근거를 보여준다. 선택 전에는 관계 목록을 제공한다. 로컬/GPT 추출 출처를 구분한다.
- 현재 프로젝트의 로컬 추출에 부정확한 이름·분류와 적은 관계가 확인됨. 화면 개선을 분석 품질 검증으로 간주하지 않는다.

## 관계 지도 스크롤 — 2026-09-11

- 데스크톱에서는 페이지 전체를 스크롤하지 않고 헤더/필터를 제외한 남은 높이에 관계망을 배치한다.
- 긴 관계 설명·근거만 오른쪽 패널 안에서 스크롤한다. 상시 스크롤바 트랙은 표시하지 않으며 휠·트랙패드·키보드 탐색을 유지한다.
- 관계 패널은 키보드 포커스를 받을 수 있다. 1000px 이하의 좁은 웹 미리보기는 세로 배치/페이지 스크롤을 유지해 내용을 자르지 않는다.

## 관계 설명 계약 — 2026-09-11

- 새 GPT 관계는 구체적인 행동/상태 유형, 한두 문장의 설명, 직접 서술/추론 구분, 정확한 인용문을 포함한다.
- 인용문은 실제 전달된 원고의 연속 문자열인지 검증하고 구간·문서·회차와 함께 저장한다. 문자열 일치는 의미 정확성을 보장하지 않는다.
- 두 대상의 양방향 관계 설명을 회차 순서로 표시한다. 회차 순서와 사건 시간 순서를 구분하며 자동으로 배신·관계 변화를 확정하지 않는다.
- 직접 서술도 AI가 추출한 검토 전 후보다. 기존 작가 확정 기능이 없는 관계를 확정 상태로 표시하지 않는다.
- 이전 데이터에 설명이 없으면 재분석 안내와 기존 원문만 제공하며 설명을 지어내지 않는다.
- 선택 회차 밖의 근거가 필요한 설명은 해당 필터에서 제외한다.

## Source of truth

- Status: 이전 시각 기준 보관 — 현재 시각 기준은 이 문서 상단의 2026-09-14 Figma v3 항목이다.
- Last refreshed: 2026-09-14.
- 근거: 사용자가 승인한 청록·아이보리 계획, `src/components/`의 기존 화면 경계, `docs/presentation/TECHNICAL.md`의 분석 역할 구분.
- 시안: [9개 화면 갤러리](docs/design/desktop-teal/index.html), [원본 목록](docs/design/desktop-teal/manifest.json).
- 기존 `docs/design/references/`의 보라색 시안은 보존하되 새 데스크톱 디자인의 기준으로 사용하지 않는다.
- 우선순위: 이 문서의 명시적 기능·문구·치수 > 생성 이미지의 세부 글자·아이콘·장식. 생성 이미지는 배치와 분위기 참고다.
- 요청 캔버스는 1920×1200. 이미지 생성 도구는 반복 요청에도 1586×992 PNG를 반환했다. 원본을 변형하지 않고 보존했다. 따라서 **정확한 1920×1200 이미지 납품 조건은 미충족**이다. 구현 기준은 아래 CSS 논리 픽셀 값으로 유지한다.

## Brand / Product goals

차분하고 읽기 편한 작가의 검토 도구. 원문 근거, 분석 범위, 불확실성, 작가 판단으로 신뢰를 만든다. 보라색, 과도한 그라디언트·광택, AI 정확도 점수, 오류를 확정하는 과장된 경고는 사용하지 않는다.

원고를 가져와 설정·인물·개연성·떡밥의 검토 후보를 확인하고, 근거를 읽고 판단하는 것이 목적이다. 집필 편집기, 자동 원고 수정, 팀 계정 자동 과금, 웹 원고 분석은 포함하지 않는다. 웹은 소개·다운로드이고 제품은 로컬 데스크톱이다.

주 사용자는 외부 집필 도구에서 원고를 작성하는 한국어 작가다. 성공 기준은 검토 후보를 선택해 양쪽 근거를 확인하고, 관계 지도나 떡밥으로 이동해도 판단 맥락을 잃지 않는 것이다.

## Information architecture

최초 실행: 시작 안내 → AI 연결·준비 → 내 작품 → 원고·설정 → 분석 → 검토 결과.

작품 내부 메뉴는 **원고·설정 / 분석 / 검토 결과 / 관계 지도 / 떡밥 관리** 순서로 고정한다. 상단 작품 이름은 메뉴와 구분한다. 하단은 내 작품·앱 설정이다. 시작 안내와 준비 화면에는 작품 메뉴가 없다. 내 작품은 내 작품·앱 설정만 제공한다. 앱 설정 내부 항목은 AI 연결 / 로컬 모델 / 저장·데이터 / 화면·접근성 / 앱 정보다.

원문·관계·떡밥·검토 후보 사이에 바로 이동한다. 복귀 시 선택 항목·필터·스크롤을 유지한다. 작품 생성·가져오기·삭제 확인은 모달이며 이번 이미지 수에 포함하지 않는다.

## Visual language

| Token | 값 | 용도 |
|---|---|---|
| background | #F7F5EF | 전체 아이보리 배경 |
| surface | #FFFDF8 | 문서·패널·카드 |
| primary | #24635B | 주요 버튼·선택 글자·포커스 |
| selected | #E6EFEB | 선택 배경 |
| text | #252A27 | 본문 |
| muted | #626B65 | 보조 설명 |
| border | #DADFD8 | 구분선 |
| warning | #8A5A20 | 확인 필요, 확정 오류가 아님 |
| danger | #AD443B | 실패·삭제 |

Pretendard를 로컬 번들로 사용한다. 파일은 `docs/presentation/fonts/PretendardVariable.woff2`, 동봉 라이선스 유지. 폰트 fallback은 system-ui, sans-serif다. 이미지 안의 한글이 실제 Pretendard임을 보장하지 않는다.

| 요소 | 크기 / 굵기 / 행간 |
|---|---|
| 페이지 제목 | 28px / 700 / 1.35 |
| 영역 제목 | 20px / 600 / 1.4 |
| 본문·입력·메뉴 | 16px / 400–500 / 1.6 |
| 보조 설명·배지 | 14px / 400–500 / 1.5 |
| 원문 | 17px / 400 / 1.8 |

간격 토큰 4·8·16·24·32·48px, 기본 그리드 8px. 버튼 높이 40px, 입력 44px, 클릭 영역 최소 40px. 카드 내부 24px, 패널 사이 16px, 페이지 바깥 24–32px. 모서리 8px, 배지 16px. 얇은 테두리를 기본으로 하고 그림자는 모달에만 약하게 사용한다.

메뉴 폭 216px, 후보 목록 280px, 원문 근거 패널 360px, 중앙 본문 가변. 원문 한 줄 길이 약 55–75자, 과도하게 넓은 본문은 최대 폭을 둔다. 아이콘은 기존 lucide-react를 활용해 20px/1.75px 선으로 통일한다. 아이콘 이름이 이미지마다 다르더라도 구현에서는 같은 메뉴에 같은 아이콘을 사용한다.

상태 전환 120–180ms. 분석 대기 시 작업 단계·완료 구간 수를 표시한다. 장식 애니메이션은 제외하고 reduced-motion을 따른다.

## Page contracts and representative states

| 시안 | 대표 상태 / 필수 동작 |
|---|---|
| 01 시작 안내 | 서비스 설명, 3단계 사용 순서, 로컬 저장·외부 전송 안내, 시작하기 |
| 02 AI 연결·준비 | GPT 연결 완료·모델 A 예시, 로컬 임베딩 모델 다운로드 62% 예시. 두 항목 준비 전 작품 시작하기 비활성 |
| 03 내 작품 | 봉인의 기록·달빛 서점·샘플 작품 목록, 상태·작품 열기, 새 작품 만들기 |
| 04 원고·설정 | 설정 메모 편집. 왼쪽 문서 목록, 중앙 세계관 규칙, 오른쪽 확정/구상·재분석 상태 |
| 05 분석 | 회차 선택 1·2·7화, 모델 A 예시, 점검 항목 세 가지, 확정 설정 3개, 참고 범위 안내 |
| 06 검토 결과 | 봉인검 사용 조건 후보, 설명·부족한 정보, 1·2·7화 인용, 문제 있음/문제 아님/판단 보류 |
| 07 관계 지도 | 유나→봉인검 사용 관계 선택, 규칙·계약 거절 사건, 근거·검토 결과 이동 |
| 08 떡밥 관리 | 사라진 편지의 3화→5화→9화 회수 후보, 작가 상태 진행 중, AI 제안 확인 필요 |
| 09 앱 설정 | AI 연결 탭, 기본 모델·재연결·해제, 원문 전송 안내, 로컬 모델 하나의 준비 상태 |

### 원고와 설정

원고 본문은 읽기 전용이다. 설정은 파일 가져오기 또는 직접 작성이며 직접 작성 메모만 앱에서 편집한다. 파일 교체와 새 회차 추가는 별도 동작이다. 가져온 파일은 로컬 작품 저장소에 복사하고 외부 파일을 덮어쓰지 않는다. 확정 설정은 검토 기준, 구상 메모는 참고다. 확정 설정끼리 충돌해도 임의의 우선순위를 적용하지 않는다.

목록은 원고/설정 구분을 유지한다. 생성 시안 04는 전체 맥락을 보여주기 위해 두 목록을 함께 노출했지만, 구현에서 탭을 사용한다면 선택 탭의 문서만 표시한다. 이 결정은 이미지의 시각적 예시보다 우선한다.

### 모델과 분석

기본 구성은 **로컬 임베딩 모델 1개 + 검색 코드 + 로컬 저장소 + 외부 사용자 GPT**다. 로컬 모델명은 상태 설명으로 보여 주고 초기 버전에는 다중 로컬 모델 선택 UI를 제공하지 않는다. 시안 02의 모델 필드 오른쪽 화살표는 생성 이미지의 편차이며 구현에서는 읽기 전용 필드다.

사용자의 연결 경로에서 실제 이용 가능한 GPT 모델만 선택지에 표시한다. 예시 문자열은 `사용 가능한 모델 A · 예시`. 기본 모델은 설정에서 지정하고 이번 분석 모델은 분석 화면에서 변경한다. 실행 중 모델을 자동으로 바꾸지 않는다. 모델 변경은 새 분석 또는 사용자가 승인한 재개 구간에 적용하며 결과에 모델 이력을 남긴다. 구독 연결·지원 모델·사용량 조회 가능 여부는 기술 실증 전이다.

전체/변경/회차 선택 제공. 최초 기본값은 전체, 이후 변경된 원고. 관련 근거 검색은 선택 외 회차도 참고할 수 있음을 표시한다. 최초 추출에서는 원고 전체가 구간별로 외부 전달될 수 있다. GPT에 숫자 벡터만 전송하는 구조가 아니다.

### 판단과 그래프

AI 검토 후보와 작가 판단을 별도 보존한다. 문제 있음은 수정 대상으로 기록할 뿐 원고를 수정하지 않는다. 문제 아님은 기본 대기 목록에서 제외하되 재열람 가능하다. 판단 보류는 보류 상태로 남긴다. 설정 교정은 별도 동작이다. 근거와 버전이 바뀌면 재검토 필요를 표시한다.

관계 지도는 인물(원), 아이템(둥근 사각형), 규칙(사각형), 사건(마름모)과 이름을 함께 사용한다. 선에는 방향과 동사를 표시한다. 의미가 없는 일반 ‘관계’는 ‘유형 미분류’로 표시하고 방향 화살표를 생략한다. 명시적 단절은 적갈색의 굵은 선과 건강도 배지로 표시하며, 중간 공백 후보는 황갈색 점선으로 표시한다. 단순 미언급은 단절로 그리지 않는다. 여러 후보가 같은 두 대상을 잇는 경우 캔버스에는 근거·신뢰도가 높은 대표선만 그려 겹침을 줄이고 후보 수와 양방향 여부를 표시하며, 전체 후보와 원문은 근거 패널에서 유지한다. 기본 화면은 핵심 관계망이며, 선택한 대상의 주변 관계 보기와 전체 관계망 복귀를 제공한다. 확대·축소·화면 맞춤·자동 배치 다시 실행을 제공한다. 시점이 다른 관계를 하나로 덮어쓰지 않는다.

떡밥 후보는 미검토 상태에서 작가가 채택·제외한다. 채택된 떡밥 상태는 진행 중/회수 확인/의도적 미회수. 마지막 언급 이후 긴 공백만으로 오류 처리하지 않는다. 회수 후보와 작가가 확인한 회수를 구분한다.

## Components / implementation handoff

Sidebar, Inspector, GraphView, SetupPanel, AnalysisProgressPanel은 현재 구현의 책임 경계다. GraphView는 연결 컴포넌트 선택기, 끊긴 끝점 고스트, 대표 관계선, 건강도 경고, 근거 패널 진입점을 제공한다. 공통 페이지 헤더·상태 배지·근거 카드·모델 선택기·후보 목록·준비 카드·타임라인을 재사용하며 색상·치수의 소유권은 공통 토큰에 둔다.

## Interaction states

| 상황 | 표시와 다음 행동 |
|---|---|
| 첫 준비 | GPT 연결과 모델 설치를 별도 진행. 준비 완료 뒤에만 최초 작품 진입 |
| 모델 다운로드 | 실제 바이트 정보를 알 때만 용량·백분율 표시. 실패하면 재시도, 이미 받은 파일은 검증 후 재사용 |
| 인증 실패·만료 | 원인과 재연결. 토큰·개인 정보는 노출하지 않음 |
| 분석 진행 | 원고 준비→검색 자료 준비→사실·관계 정리→문제 검토. 현재 회차·완료 구간 수·중단 |
| 중단 | 완료한 작업 보존, 미완료 표시, 재개 선택. 성공으로 표시하지 않음 |
| 한도 소진 | 자동 모델 변경이나 팀 계정 대체 없음. 다른 이용 가능 모델 선택 또는 나중에 재개 |
| 부분 실패 | 완료·실패 범위를 분리 표시하고 실패 구간 재시도 |
| 원고 변경 | 이전 결과임을 표시하고 관련 결과 재분석. 작가 교정 보존 |
| 작품·문서 없음 | 새 작품 만들기·파일 가져오기 행동을 하나의 기본 버튼으로 안내 |
| 후보 없음 | “이번 분석 범위에서 검토 후보를 찾지 못했습니다.” 오류 없음이나 100% 정상으로 단정하지 않음 |
| 네트워크 없음 | GPT 분석 불가 이유 표시. 최초 준비 이후에는 기존 원고·결과·로컬 기능 유지 |
| 파괴적 작업 | 작품 이름·범위 확인. 검색 자료 재구축과 작품 삭제를 분리 |

## Accessibility / responsive behavior

일반 글자 대비 4.5:1, 큰 글자·주요 컨트롤 3:1을 구현 시 목표로 측정한다. 이미지의 시각 검토가 접근성 테스트를 대체하지 않는다. 포커스는 청록 2px 외곽선과 2px 간격. 모든 컨트롤에 이름을 제공하고 키보드로 목록·탭·모달 조작 가능하게 한다. 모달은 포커스 복귀와 Escape 닫기를 지원한다. 색만으로 상태를 구분하지 않는다.

1920×1200은 기준 캔버스다. 1440px 이상 3열, 1200–1439px에서 근거는 열고 닫는 패널, 그 미만에서 메뉴는 64px 아이콘 레일·라벨 툴팁과 패널 전환 사용. 1024px까지의 데스크톱 작업을 목표로 한다. 글자 확대 시 본문 가로 스크롤보다 패널 전환을 우선한다. 모바일 앱 시안은 범위 밖이며 갤러리만 모바일 열람에 대응한다.

## Content voice / exact copy

간결하고 중립적인 한국어. “설정 오류 발견”보다 “사용 조건 확인 필요”. “검색 점수 90%”를 정확도로 표시하지 않는다. 불확실한 정보를 확정형으로 서술하지 않는다.

공통 샘플: 작품 봉인의 기록, 인물 유나, 아이템 봉인검. 1화 “계약자만 봉인검을 사용할 수 있다.” / 2화 “유나는 계약을 거절했다.” / 7화 “유나는 봉인검을 들어 올렸다.” 떡밥은 사라진 편지, 3화 등장·5화 후속·9화 회수 후보다. 이미지 안의 장식용 추가 문장·회차·맞춤법 차이는 제품 데이터가 아니며 이 문구가 우선한다.

## Verification and remaining limits

9장의 주요 제목·기능·모델 구분·외부 전송 안내·작가 판단·색상 방향을 시각 검토했다. 첫 검토 이미지의 배경 손상, 잘못 생성된 메뉴, 작품 목록의 내부 메뉴 노출, 관계 지도의 메뉴 누락, 검토 판단 버튼 문구를 이미지 생성 편집으로 교정했다.

이미지는 글자·아이콘·미세 색상·크기를 픽셀 단위로 통일한 구현 스크린샷이 아니다. 캔버스 해상도 제한과 샘플 장식 문구 편차가 남아 있다. 실제 앱의 설치·인증·모델 추론·접근성·반응형은 별도 검증 항목이며, 현재 구현은 관계 지도·저장·분석 흐름을 계속 보완하는 중이다.

공개 배포 전 macOS 앱은 `APPLE_SIGNING_IDENTITY='Developer ID Application: ...' scripts/sign-macos-app.sh`로 서명·검증한다. 공증 자격 증명이 준비되면 `NOTARIZE=1 APPLE_NOTARY_PROFILE='프로필명'`을 함께 지정해 `notarytool submit`과 `stapler validate`까지 실행한다. 인증서가 없는 개발 환경에서는 앱·DMG가 unsigned로 생성되며, 이를 서명된 릴리스로 표시하지 않는다.

## 실제 앱 적용 — 2026-09-10

- `src/workbench.css`에서 청록·아이보리 토큰과 로컬 Pretendard를 적용한다.
- 고정 작품 메뉴를 두고 원고·분석·검토 결과·관계 지도를 별도 화면으로 제공한다. 원고 화면은 읽기 전용이다.
- 검토 결과는 후보 목록, 설명·작가 판단, 원문 근거로 나눈다. 좁은 창에서는 근거를 설명 아래에 배치하고 본문을 스크롤한다.
- 관계 지도는 독립 캔버스와 근거 패널로 구성한다. 이름·별칭 검색은 해당 노드와 직접 연결된 이웃을 함께 표시한다. 관계선 선택으로 원문 근거를 연다. 연결 컴포넌트는 중심성이 높은 대상을 기준으로 계층 배치하고, 여러 섬은 선택기로 개별 확대한다. 필터 변경 시 화면에 다시 맞춘다.
- 분리된 관계망 선택기는 대상 수·관계 수와 문제 관계 수를 함께 보여주고 현재 선택된 망을 활성 상태로 표시한다. 인물은 원형, 아이템은 둥근 사각형, 규칙은 사각형, 사건은 마름모, 떡밥은 육각형으로 구분해 색상에만 의존하지 않는다.
- 분석 화면은 기존 개인 GPT 연결, 모델·추론 강도, 원문 전송 동의 및 결과 재사용을 유지한다. 로컬 생성 모델은 고급 항목이다.
- 설정 메모는 작품별로 저장하며 `확정 설정`과 `구상 메모`를 구분한다. 확정 설정만 충돌 판정 기준으로 사용하고 구상 메모는 참고 후보로 표시한다. 떡밥 후보는 `검토 전·진행 중·회수 확인·의도적 미회수` 상태를 작가가 저장하며, 단순 미언급만으로 오류 처리하지 않는다.
- 이번 확인은 기존 저장 분석 결과를 이용한다. 추가 GPT 분석을 실행하지 않는다.
- 분석 화면은 검색 준비 상태를 전체 폭으로 먼저 보여주고, 분석 대상과 GPT 설정을 남은 공간에 나란히 배치한다. 작업 공간이 좁아지면 두 패널을 한 열로 전환한다.
- macOS 기본 창은 1440×900으로 시작하고 1100×720까지 줄일 수 있다. 콘텐츠 영역은 창 폭을 채우며, 고정 폭 본문으로 오른쪽 빈 공간을 만들지 않는다.

## 원고 탐색·설정 저장·처리 기록 보완 — 2026-09-12

- 원고 목록은 회차·제목 검색과 12편 단위 페이지 이동을 제공한다. 근거에서 회차를 열면 해당 회차가 있는 목록 페이지로 이동한다. 목록의 페이지 이동만으로 읽는 원고를 바꾸지 않는다.
- 설정 제목·내용·확정/구상 선택에는 명시적인 레이블을 제공한다. 저장 중에는 입력과 중복 저장을 막으며, 성공한 새 메모만 입력을 초기화한다. 실패 시 입력과 편집 화면을 유지하고 재시도 안내를 표시한다. 편집 취소 후 다시 열면 저장된 값에서 시작한다.
- 작품을 변경하면 해당 작품의 설정 폼으로 초기화한다. 이전 작품의 늦은 저장 응답으로 현재 작품의 목록을 덮어쓰지 않는다.
- 분석 처리 기록은 확인 필요·검증 완료·미실행·전체 필터와 8개 단위 페이지 이동을 제공한다. 상세 목록 안의 별도 고정 높이 스크롤은 없애고 페이지 스크롤을 사용한다. 분할된 구간은 펼쳐서 확인하며 앞부분·뒷부분, 오류, 시도 횟수와 저장 결과 재사용을 구분한다.
- 실패·부분 완료도 흰색 카드와 청록·아이보리 기본 체계를 유지한다. 상태는 제목·아이콘·테두리로 함께 구분한다. 설정 상태 배지는 단어 중간에서 줄바꿈하지 않는다.
- 이번 검증 범위: 실제 컴포넌트를 사용한 가상 원고 500편·처리 기록 120개의 헤드리스 브라우저 조작, 폭 1440/1024/800px 캡처 및 가로 넘침 검사. macOS 앱 빌드는 확인했으나 잠긴 Mac의 네이티브 조작이나 실제 GPT 분석 품질을 검증한 결과는 아니다.

## 원문 근거로 이동 — 2026-09-12 후속

- 목록 안에서 회차를 선택해도 검색어를 유지한다. 외부 근거에서 특정 회차를 열면 검색 필터를 해제하고 해당 회차의 목록 페이지를 보여준다.
- 같은 회차·같은 인용문을 다시 클릭해도 매번 이동 요청으로 처리한다. 작품 전환 시 이전 작품의 근거 요청은 지운다.
- 검토 결과·검토 이력·관계 설명·관계의 전체 근거에서 원고를 열 때 인용문을 함께 전달한다. 청킹 과정의 줄바꿈·연속 공백 차이는 비교할 때만 정규화하고, 원본 위치로 되돌려 원문의 문단·공백을 그대로 강조한다. 단어·문장부호 변경은 일치로 취급하지 않는다. 같은 문장이 여러 곳이면 모두 강조하고 위치가 여러 곳이라는 안내를 제공한다. 수정 등으로 일치하지 않으면 임의의 문장을 강조하지 않고 안내한다.
- 회차를 선택하면 본문의 시작으로, 근거를 선택하면 첫 일치 위치로 이동하고 키보드 포커스를 옮긴다. 긴 본문에서도 회차 제목·원고 목록 복귀 버튼을 유지한다. 목록 복귀는 검색창에 포커스를 돌려준다.
- 가상 데이터 브라우저에서 회차 선택·반복 근거 열기·검토와 관계 근거 이동·원문 수정 불일치·작품 전환을 검증했다. 실제 Mac/GPT 분석 품질과는 별도 결과다.

## 일반 창 높이에서 관계 지도 — 2026-09-12 실데이터 점검

- 관계 건강도는 상태와 0이 아닌 점검 수를 요약하고, 상세 항목은 펼쳐서 확인한다. 지도 읽는 법과 범례도 펼침 영역으로 제공한다.
- 데스크톱 폭(1001px 이상)에서 `지도 넓게 보기`는 제목·검색·회차·유형 필터를 접고 캔버스와 근거 패널을 유지한다. 복귀 버튼 또는 Escape로 원래 화면으로 돌아간다.
- 화면 맞춤의 여백은 캔버스 크기에 비례하며 20–56px 범위를 사용한다. 노드 수가 적은데도 고정 여백으로 지도가 지나치게 축소되는 문제를 줄인다.
- 실제 데스크톱 DB 복사본을 연결한 인앱 브라우저에서 전체 관계, 고립 후보 선택, 회차별 근거 이동을 확인했다. 네이티브 실행과 새 GPT 분석 품질 검증은 별도다.

## 분석 대상과 빈 검토 결과 — 2026-09-12 후속

- 분석 대상은 원고 화면과 같은 회차·제목 검색 및 12편 단위 목록을 사용한다. 페이지 이동은 목록 위에 둔다. 회차를 누르면 해당 원고를 열며, 검색으로 GPT 분석 범위가 바뀌지 않음을 명시한다.
- 신규 원고는 아직 분석하지 않음, 수정 원고는 수정 후 재분석 필요로 구분한다.
- 확인 대기와 작가 판단 탭 중 하나만 비어 있으면 다른 탭의 기존 후보로 안내한다. 전체가 비었을 때만 원고 유무와 미분석 상태에 따라 원고 관리 또는 분석 상태 확인으로 안내한다. 후보 없음은 오류 없음이나 분석 성공을 뜻하지 않는다.
- 작품을 변경하면 검토 탭은 확인 대기로 초기화한다.
- 복사본에 만든 500개 짧은 문서로 목록 검색·페이지 이동·원고 열기를 인앱 브라우저에서 검증했다. 대용량 원문 전송·임베딩·GPT 분석 성능을 검증한 것은 아니다.

## 시작 실패와 복구 — 2026-09-12

- 초기 조회의 일시적인 연결 오류·시간 초과는 총 3회까지 재시도한다. 계속 실패하면 요청·시도 횟수와 다시 확인 버튼을 표시한다. 무한 로딩하지 않는다.
- 작품 데이터는 관련 조회가 모두 성공한 뒤 함께 반영한다. 실패 안내를 준비 완료 문구로 덮지 않는다.
- 진단 로그는 요청 메서드·경로·시도·오류 종류만 기록하며 인증 정보와 원고를 포함하지 않는다.
- 인앱 브라우저에서 연결 장애 주입·재시도 복구를 확인했고, 원래 앱에서도 /projects 일시 실패 후 자동 복구를 관측했다. 네이티브 실행과 GPT 분석 완주 검증은 별도다.

## 작품 전환·조회 실패 격리 — 2026-09-12

- 작품 전환 시 이전 작품의 설정·검토 이력·근거·떡밥 상태와 선택 상태를 함께 초기화한다. 새 작품의 조회가 끝나기 전에는 로딩을, 실패하면 다시 시도/내 작품으로 이동을 제공한다. 조회 실패를 빈 원고나 후보 없음으로 표시하지 않는다.
- 같은 작품을 새로 조회할 때는 마지막 성공 자료를 유지한다. 회차 선택과 실제 지도 표시 범위를 구분하고, 실패 시 이전 범위를 보고 있음을 알린다.
- 인앱 브라우저의 실제 API 응답 중단과 지연으로 5개 페이지의 실패 안내, 500편 목록 복구, 이전 응답 무시를 확인했다. 원본 DB는 변경하지 않았다.

## 판단 저장·작품 이동 — 2026-09-12

- 검토 판단 저장 중에는 네 판단 버튼을 잠그고 진행 문구를 표시한다. 실패 시 후보 옆에 저장을 확인하지 못했다는 안내와 다시 선택할 수 있는 버튼을 제공한다. 응답 실패만으로 서버 저장 상태를 단정하지 않는다.
- 설정·떡밥·판단의 늦은 응답은 다른 작품이나 이전 방문 이후의 화면에 반영하지 않는다. 같은 항목의 진행 중인 저장은 작품 재방문 시에도 중복 실행하지 않는다.
- 실제 인앱 브라우저에서 판단 저장 지연·실패·재시도·새로고침 후 유지까지 검증했다. 설정·떡밥은 동일 보호를 적용한 범위이며 네이티브/GPT 분석 검증과 구분한다.

## 분석 중 작품 탐색 — 2026-09-12

- 다른 작품의 분석이 진행 중이면 작품명과 진행 상황 보기 버튼을 표시한다. 현재 작품은 읽을 수 있고 추가 분석은 진행 작업 종료 후 시작한다.
- 작품에 진입하면 해당 작품의 작업 상태를 조회해 진행·완료·실패를 복원한다. 완료 기록을 짧은 타이머로 숨기지 않는다.
- 요청 응답이 실패했어도 작업이 진행 중이면 상태 확인을 유지한다. 작업 종료 후 문구·버튼을 갱신하고, 결과 조회 실패 안내를 완료 문구로 덮지 않는다.
- 이번 검증은 통제된 분석 응답을 사용하는 인앱 브라우저 UI 시험이다. 실제 GPT 분석·성능·10편 완주 검증과 구분한다.

## 원고 교체와 저장 응답 — 2026-09-12

- 같은 내용의 수정본은 기존 분석·작가 판단을 유지한다고 안내한다. 수정된 원고는 마지막 분석 해시와 비교해 `수정 후 재분석 필요`로 표시한다. 기존 분석이 없던 원고는 `아직 분석하지 않음`으로 구분한다.
- 원고 가져오기·교체·삭제의 저장 응답은 다른 작품 화면을 갱신하지 않는다. 저장이 끝나기 전에 해당 작품으로 돌아온 경우 현재 원고를 다시 조회한다.
- 파일 경로 오류는 입력창 안에 표시한다. 저장 중에는 입력·중복 제출을 막고, 창을 닫아도 저장은 계속된다고 설명한다. 주요 제출 버튼은 공통 청록색을 사용한다.
- 원고 교체 시 회차·제목·문서 ID를 유지하고, 변경 전 검토와 원문 근거는 검토 이력에서 확인한다. 마지막 분석 해시를 보존해 수정 여부를 표시하지만 현재 해시와 다른 응답은 재사용하지 않는다.
- 인앱 화면에서는 동일 파일·빈 파일·없는 파일·저장 응답 지연·실패·저장 중 재방문을 확인했다. 삭제 확인창의 UI 조작은 완료하지 못했으며, 삭제의 데이터 처리는 패키지 백엔드 API로 검증했다. 실제 GPT 분석 및 잠긴 Mac의 네이티브 조작을 대신한 검증이 아니다.

## 중단된 분석 이어서 실행 — 2026-09-12

- 실패·취소 화면은 100%로 완료된 인상을 주지 않고 검증 완료 구간 수를 표시한다. 처리 중이던 구간은 `중단`, 시작하지 않은 구간은 `미실행`으로 구분한다. 하위 구간도 같은 상태 문구를 사용한다.
- `확인 필요` 필터에 중단 구간을 포함하고, `미실행`에서 남은 회차를 확인한다. 완료 시 `검증 완료` 목록을 기본으로 연다. 목록은 8개씩 표시한다.
- 청록색 `남은 구간 이어서 분석` 버튼 앞에 저장된 모델·추론 강도를 보여준다. 재시도는 저장된 설정을 사용하며, 원고·설정이 바뀐 경우 재검토가 필요함과 새 요청의 계정 사용량을 알린다.
- 이력에 모델 설정이 없는 구형 작업은 임의 모델로 재시도하지 않는다. 분석 화면에서 모델과 강도를 선택하도록 안내한다.
- 검증은 인앱 화면·실제 분석 엔진·통제된 모델 응답으로 수행했다. 네이티브 창 및 실제 GPT 장편 분석 검증과 구분한다.
